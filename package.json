{
  "name": "grid-engine-ts-example",
  "version": "1.0.0",
  "description": "Grid Engine TypeScript Example",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "esbuild src/$npm_config_game/game.ts --servedir=. --outfile=$npm_config_game/main.js --bundle --target=es2016",
    "build": "esbuild src/$npm_config_game/game.ts --outfile=$npm_config_game/main.js --bundle --target=es2016"
  },
  "author": "Annoraaq",
  "license": "Apache-2.0",
  "devDependencies": {
    "esbuild": "^0.18.14",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "grid-engine": "^2.32.3",
    "phaser": "^3.60.0"
  }
}
